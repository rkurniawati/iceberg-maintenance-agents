# Iceberg maintainer and optimizer 

## How to run 

### Create an Apache Iceberg Datalake

You can use the iceberg provided in `./iceberg-docker`

To start:
```bash
cd iceberg-docker
docker-compose up -d
```

To start:
```bash
cd iceberg-docker
docker-compose stop -d --volumes
```